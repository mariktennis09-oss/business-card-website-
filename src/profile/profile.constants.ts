/**
 * Визитка — singleton: один профиль одного человека.
 * Резолвер адресует его по фиксированному естественному ключу, сид — по нему же
 * делает upsert, поэтому константа общая для приложения и для сида.
 */
export const PROFILE_SLUG = 'mark';
